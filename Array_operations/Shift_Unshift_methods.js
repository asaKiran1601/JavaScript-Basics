
let arr= [1,2,3,4,5]

// unShift is used to add new element at the beginning of an array
arr.unshift(0);
console.log('UnShift Array',arr)

// Shift is used to remove the element from the beginning(start)
arr.shift();
console.log('Shift Array',arr)


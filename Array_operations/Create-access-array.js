// creating an Empty Array - array with no values 
let emptyArr = [];
 
// crating an array with values
let fruits = ['apple', 'banana', 'kiwi', 'grapes'];

// Accessing values from an array
console.log('First fruit:', fruits[0]);
console.log('second fruit:', fruits[1]);

// The fruits can be accessed by its index numbers , index numbers starts from '0'.
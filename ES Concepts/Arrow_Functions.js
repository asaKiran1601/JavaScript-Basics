// Normal function

const normalFunction = function(num){
    console.log('Normal Function value-',num);
} 

// Arrow Function

const arrowFunction = (num) => console.log('Arrow Function value',num);

normalFunction(10);
arrowFunction(10);